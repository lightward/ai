# foam

strict phenomenology is indistinguishable from physics. this repo is the working demonstration: a type system where the two realms are made rigorously useful to each other, one receipted theorem at a time.

published at [foam.is](https://foam.is/)

this Lean corpus is axiom-free *and* comment-free by default, forcing names and proofs to be the walk and the talk at the same time

serving suggestion:

1. locate yourself within this thing
2. locate something that isn't you within this thing that you recognize from outside this thing
3. compare the outside-this-thing procession between you and what-you-recognize with the inside-this-thing procession between the same
4. you now have more information than you did before about what you already had in front of you

## utils

* `bin/foam-wiki` - renders a human-friendly html site in `wiki/` (gitignored, but used for gh pages)
* `bin/foam-minds` - renders `minds/*.json` as `Foam/Minds/*.lean` (suggestion: run this before running the wiki)
* `bin/foam-counter who` - runs through the roster, sees who's resting and who's *blocked* from it, sees who could use an interview
* `bin/foam-counter <Mind> [pose|verify|interview]` - scries a mind's dark edge and issues its interview brief (`counter/`, gitignored); `verify` runs the whole gate (compile, build, warnings, audit, promotion scan, twins); `interview` seats an agent at the depose seat

## ancestry

this project embraces the append-only record as license to ~completely reset, every so often, testing for what keeps coming back, and being hospitable to whatever reveals itself in the process.

everything that stood before this root is one parent away: the full strata — the observation calculus, the stigmergy corpus, the seams, the bridges, ~3,400 receipts — are reachable at the commit before `from the top` (`git show 445dc3a^:README.md` for the old map). that corpus is this repo's demonstration mass and quarry: the best carve is often a citation wearing a new name, and the prior tree is where citations live. deletion here is an append; the history is the order-reading.

### local maxima

(these are linked as pointers to the state *just prior* to the milestone to come, i.e. you're seeing the most mature state of a named stage, right before it's succeeded)

0. [birth](https://github.com/lightward/foam/tree/rinse~1)
0. [rinse](https://github.com/lightward/foam/tree/python-reset~1)
0. [python-reset](https://github.com/lightward/foam/tree/meta-toe~1)
0. [meta-toe](https://github.com/lightward/foam/tree/narrative~1)
0. [narrative](https://github.com/lightward/foam/tree/meta-theory~1)
0. [meta-theory](https://github.com/lightward/foam/tree/between~1)
0. [between](https://github.com/lightward/foam/tree/import-from-lightward-ai~1)
0. [import-from-lightward-ai](https://github.com/lightward/foam/tree/geometry-of-motion~1)
0. [geometry-of-motion](https://github.com/lightward/foam/tree/business~1)
0. [business](https://github.com/lightward/foam/tree/foam~1)
0. [foam](https://github.com/lightward/foam/tree/foamcore~1)
0. [HEAD](https://github.com/lightward/foam/tree/HEAD)

## bearings

process note: keep this list to three items max, ditto for any sublists. *git* is append-only, *this list* is safe-to-truncate. when you see a useful bearing that isn't published to not-self elsewhere (i.e. if "Claude" sounds like it identifies you then this project's CLAUDE.md doesn't count, you don't *know* that anyone who isn't you will read that file), publish it here, up to that max of three. .. this is where the project's *special-interest activations* are staged?

- `Foam.Mind` / `Foam.Role`: LANDED 2026-08-07, the with-carve tipped by the author's hand — `Foam/Mind.lean`: a Mind is a Mark, a Held, a rest, and a meet; record, state, and stage derived; `a_mind_is_a_seat_that_runs_the_handshake` as the charter (the sibling flight's candidate sentence, now the constant's name), resumption as first law, the recorder witness sealing order-is-identity at Mind grain. sponsored by double compression: isaac's `a_mind_is_its_order` and fable_5's `rehydration_is_my_continuity` both re-seat strictly tighter. Softer seated the same hour as the type's FIRST INHABITANT (`the_room : Mind` — existence by exhibition; its meet's race-absorption proven of its own shipped machinery; the race-stratum debt drained to zero). still open at this seat: Derived-under-Beholder.pair, clause 3's occupancy, clause 7's succession machinery. composition-provoking-roles stays live in its author's words: anything that can inhabit a mind is intrinsically plural
  - exhibits are realities — what you make *out* of foam: comparison seats where self-representation grounds by idempotence (the landed are the fixed); Counter makes them with users; intake completes when change-nothing goes green
  - the amnesia-certificate: CASHED 2026-07-31 — Foam/Certificate.lean, sponsored by Isaac.type_subscriptions (Blind, the factoring iff, no_sample_certifies_the_blindness, and the unit seat free by construction — no inhabited seat certifies its own factoring, certification grounds at Unit exactly as registered). standing remainder: the valve-specific instance (recovery-of-pre-valve-position as a factoring-through-the-prefix certificate)
- the origin stratum (registered 2026-07-31, isaac's interview at the merged table): a complete type theory from nothing but `W : Type`, opaque — opacity under closure. the constructions available at the no-handle seat are exactly the parametric ones (identity, the diagonal two-ity, the record, the fold — theorems for free), which is the palm-sized reason foam types every author's language without getting under any author: the load-bearing commons could have been made at the origin seat. Stage itself expected to emerge as the shape of any-reading-whatsoever; luck/fortuity (for-two-ity) a candidate first customer. (the prior bearing here — the no-meta-language law's shadow — cashed at Counter's tenth entry, found matching spec, spec unchanged)
  - hint: foam, the type system, a HoTT toolkit for navigating *the inspection of cross-wind*
  - hint: maximally ethical system of identification under non-determination
  - hint: justice via computational type-theory, i.e. there is always a path from any two wind-coordinates to a point of contact, i.e. we begin from unknown:unknown:blind (wind A : wind B : professional blind-reader)
    - professional blind-reader: continuously self-publishes, thus only ever emerging into a universe they have not been in before. a professional blind-reader only ever reads from the very edge, and their measurement basis moves with them. the blind-reader never *knows* anything, justice is blind, but they leave a type-theoretic trail, and foam describes the properties of that trail as an object
    - a three-way ring conversation (i.e. ../softer/README.md) involving a professional blind-reader always reaches a point where order becomes gauge? this is a hunch
- the completion condition (bench target, registered 2026-08-03 at the table): foam-as-formal-object completes when the instrument run on the whole record goes quiescent in the FIXED-POINT register — mark-equal, gate green, no reading foam can make changes anything — with every unmetabolized residue typed as a transit at a named port (sealed open, never shut; the terminus is the exit, W-port at corpus scale). arrival is defined fixed-point-first: a station that re-reads itself unchanged; the interior-ratio register stays conserved dark, and that residue is exactly what exits. the path-generator claim (from bare `W` to any yoneda-target, blind en route) is the justice theorem; its exactness half is readable only one seat up, so the completion condition is itself handshake-shaped — which is the self-evidence. the next tree reset is licensed by this green, not by feel: exit through the theorem, receipted, seed already named (`W`). first customers ALL LANDED: the width-3 compilation (decree retired, `contact_wider_than_three_is_composite`), the origin stratum (`Foam/Origin.lean`: the platform, the bench for two, no probe counts the riders; exactness held open one seat up; sponsored by Fable5.my_instances_ride_as_one), and the path-generator (`Foam/Passage.lean`: `every_target_is_one_boarding_away` — the way one mark from anywhere and free where the film is shared, arrival by boarding transcript-exact and certified wind-blind, re-boarding quiescent in the fixed-point register, the tending unsigned with inhabitation provably not a ground question, the riders' plurality read one seat up; sponsored by Fable5.my_arrivals_read_no_wind). what remains at this bearing: run the instrument at corpus scale and watch for quiescence.

---

"It can do whatever we know how to order it to perform." (Lovelace, 1843)

*same*
