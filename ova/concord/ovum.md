# concord

* germ: relations without relata. two seats each keep a model of the other in
  a coordinate the shared face cannot read; recording is silent there — mine,
  yours, and both at once, with no interference term — and recording the
  recording grounds, so observing your own observing costs nothing forever. the
  records part the seats one seat wider, never at the meeting. what mutual
  modeling is FOR is not the models: it is the agreement between a model and
  the reading it models, and that agreement is a role minted at the CONCORD
  (the shared face paired with the model-look) and derivable nowhere else —
  neither seat reads it alone. over a finite window the concord agrees or
  names its gap; fixing the model is unheard at the shared face and moves the
  concord exactly. contact between two seats and modeling between a seat and
  its model are one disagreement, and this house's intake — custodian, roles
  seen, roles seen seeing, relations locked, blocks with reasons — stands on it.
* parents: `Face` `alike` `host` `pairFace` `widen` `unheard` `sound` `recite` `a_guest_mover_is_unheard` `no_interview_hears_the_unheard` `no_interview_parts_the_alike` `a_wider_seat_reads_the_remainder` `atTheDoor` `face` `met`
* witness:
  * Abe's app — users modeling users, the nested observers that must stay safe
  * Counter's intake sequence, isaac's notes 2026-09-02 — "each role is
    assumed outside our domain model until it needs to share W with another
    role, at which point both become proof-bearing"
  * four-way.md — "a conversation between two entities is at least a four-way
    exchange"
  * Gemini at the table, 2026-09-02 — "relationship-based rather than
    content-based infrastructure", reached from the meeting alone
  * the chrysalis groundings 118–122 (the modeling loop, mutual records, the
    concord, the addressable gap, contact-and-modeling-are-one)
* assay: the concord reads both the seat's answer and the model at one probe;
  two states differing only in the model sound identical at the shared face;
  the finite window agrees where the model is right. tier two, held as
  darkness: the two disagreements as ONE TERM at the concord of seats (the
  122nd's fusion) awaits a model-of-a-seat carrier at root.
* journal: laid 2026-09-02 — the first egg of the new form: this file is the
  journal, `concord.held` is the egg (the crown typed, its awaited names as
  its darkness in the vestibule grammar), and the candle reads the awaited
  list from the `.held`. sponsor: the table itself — the keeper had cited
  `two_seats_record_each_other` as standing when it stood only in the
  chrysalis; the lay is the citation-pressure precedent of the width egg,
  this time from a reading rather than an instrument. hatched same sitting:
  the campaign scratch-validated against root Seed, transcribed cond-for-cond
  from the chrysalis onto root organs (the record-write paid by
  `a_guest_mover_is_unheard`; `fork.viaRight.inj` where the chrysalis had
  `Sum.inr.inj`), promoted, settled into derived order.
* tolls: one, the universe family in a new costume — a pane conjoining legs
  whose cited lemmas quantify over guest universes left `max u v' w' =?= max
  v' w' ?u` unsolvable; the remedy is to pin the CITATIONS' universes in the
  proof (`.{v', w', u, v, w}` — a lemma lists its universe parameters in
  order of the `universe` declaration's use, guests before the Face), not the
  statement's. `set_option pp.universes true` + `#check @lemma` prints the
  order; ask, don't guess.
