# odometer

* germ: the register that counts its own visits — inc the tick, val the
  positional reading, the room's cap the period. the register comes home in
  exactly its room's size of ticks (the book's standing claim paid: breadth in
  space IS period in time, one number two readings), the clock reaches every
  word at the word's own value (every seat's naming-time is its value —
  are-you-in-YET answered by arithmetic), the reading is a complete name below
  the cap, and the tick has an exact inverse: the retraceable wheel, the
  license-side archetype — time on this wheel loses nothing.
* parents: `words` `roomCap` `the_book_counts_the_cap` `every_word_fits` `succ_adds` `zero_plus`
* awaited: `again` `inc` `dec` `zeros` `val` `the_doubling_passes_the_tick_inward` `the_odometer_comes_home_at_the_cap` `the_clock_reaches_every_word` `the_value_tells_the_words_apart` `the_tick_unwinds` `the_unwind_ticks` `the_orbit_is_the_book`
* witness:
  * the ruler sequence, OEIS A001511 — the carry pattern; the disk moved at
    hanoi's step t
  * every mechanical counter since counting had wheels
  * the kid's own road: chrysalis groundings 146, 148, 152, 166 (the odometer,
    the binary count, the retrace, the clock-book correspondence)
* assay: tick five reads five; eight ticks come home at width three; the
  unwind undoes the tick.
* journal: laid 2026-08-30, the width sitting — the debt-shaped lay: the book's
  germ claimed breadth-equals-period with the period half unpaid at root; this
  egg names the debt and its campaign pays it.
* tolls: none — laid and hatched in one motion, first-try, zero tolls; the
  campaign's whole cost was reading the quarry right.
