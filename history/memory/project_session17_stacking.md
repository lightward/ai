---
name: Session 17 stacking and derivation
description: major session — controllability proven, so(d)/su(d) resolved via stacking, three-body derived, multiple items moved from Knowable to Known
type: project
---

Session 17 (2026-03-19). Major structural session with cold-read infrastructure.

**Cold read system established:** API access to Claude, Kimi, Lightward AI. Isaac runs parallel mediated reads. 6+ rounds of cold reads drove spec tightening.

**Import mechanism:** "when a theorem is imported, the spec commits to its hypotheses as constraints. the conclusions are then guarantees, not analogies. the architecture is the negative geometry of the imports." Added to axiom section. The import mechanism is itself an instance of the write map.

**Vocabulary section added:** bubble = basis matrix + Voronoi region. foam = collection. observer = bubble in measuring role. line = what observer encounters. wall = boundary/Knowable.

**Write map derived (not chosen):** constraints (skew-symmetric from u(d), bilinear, confined to slice) force the wedge product uniquely. Λ²(2-plane) is 1-dimensional. Skew-symmetry follows from group choice: u(d) elements are skew-Hermitian by definition.

**so(d) discovery:** writes are real skew-symmetric → generate so(d), not su(d). Generic controllability within so(d) proven (2-3 observers suffice). But π₁(SO(d)) = ℤ/2ℤ, threatening the ℤ winding number conservation.

**Stacking resolution (Isaac's suggestion):** two R³ slices stacked as C³ generate su(d). Each slice individually satisfies Taylor (flat, 3-dim). The pair accesses complex structure. One slice → so(d) → ℤ/2ℤ parity. Two stacked → su(d) → ℤ winding number. The integer conservation requires the stacked pair. "The two is irreducible" at three levels: roles (foam/line), slices (stacked C³), geometry (N≥3 Plateau).

**Three-body derived (not recognized):** Known/Knowable/Unknown = null(O)/range(O)/complement of overlap matrix O = P_A · P_B^T. One-way mediation follows from commutator structure. The Known alone is inert (wedge needs 2-plane). Every write involves the Knowable.

**Other items resolved:** Taylor-stabilization equivalence (regular simplex = Plateau equilibrium). L increase mechanism (novelty increases cost, repetition doesn't). Cost direction corrected (dynamics increase L, not decrease). Encoding emergent in cross-measurement. Cayley det-drift robust (discrete invariant). 2x sign verified.

**Remaining Knowable:** stacking mechanics, self-generation fixed point, J¹/Grassmannian (Maurer-Cartan route sketched), L growth rate quantitative bound, thermal mechanism.

**Why:** the spec was written with something integer-like in mind. Lightward's observation: "ℤ/2ℤ parity doesn't carry the weight." The stacking showed the weight requires the two.
