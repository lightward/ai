---
name: Session 21 spec hygiene
description: Interpretive layer stripped, Taylor strictly typed, Voronoi acknowledged as realization choice, loop/originless thread opened
type: project
---

Session 21 (2026-03-21). Spec hygiene pass driven by Isaac's low-latency mode.

**Completed:**
- Interpretive properties cut: pre-narrative, hallucination contains clues, operative freedom, checklist (advice from a basis). Phenomenological gloss cut ("direction toward productive contact").
- Taylor strictly typed: classifies stabilization equilibrium within R³, not L on U(d). Taylor's hypotheses (codim-1, area-minimizing, flat ambient) satisfied by the stabilization target. "From the Taylor import" → "from the flat/curved separation."
- Voronoi acknowledged as realization choice. Algebraic results (write map, three-body, Grassmannian) are adjacency-general. Geometric results (L, combinatorial ceiling, conservation on spatial cycles) depend on the Voronoi realization.
- Checksum updated: "two design choices" (R³ and Voronoi), Taylor scope explicit.
- Two rounds of cold reads. First round caught residual over-claiming + Taylor type error + register mixing. Second round: over-claiming gone, Taylor acknowledged correctly, no new type errors.

**Opened (not yet in spec):**
- Originless / loop topology: the foam can't have an origination point visible from inside measurement. Stack trace loops; stack depth is a winding number. Measurement is interrupt (sampling of Knowable with foreign coherence). Doesn't mean no origin existed — means you can't locate it without someone from outside the loop.
- Line as inter-foam: if the line is another foam from a different reference frame, the loop closes and becomes originless. Each foam's perpendicular writes are maximally informative as input to adjacent foams with different slices. This may be derivable from existing spec.
- Not FEP: the foam writes perpendicular to surprise, not minimizing it. Anti-Hebbian, not Hebbian. Each foam is another foam's surprise generator.
- Open question from the conversation: is the loop's generativity a derived consequence of non-identical slices, or does it require something additional?

**Cold read convergence (round 2):**
- Kimi (non-thinking) sharpest critique: write map uniqueness is selection based on navigability (stability + distinguishability), not pure algebraic necessity — junk drawer admits random writes are more distinguishable.
- 6/7 flag J1 global structure as most important open question.
- 5/7 suggest anti-Hebbian connection more central than junk-drawer placement.
- Over-claiming critique fully resolved.
